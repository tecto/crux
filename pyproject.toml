[project]
name = "crux"
version = "0.1.0"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.coverage.run]
source = ["scripts/lib"]
omit = ["tests/*"]

[tool.coverage.report]
fail_under = 100
show_missing = true
